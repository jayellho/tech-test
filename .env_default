STT_MODEL=facebook/wav2vec2-large-960h
SAMPLE_RATE=16000

# Host configuration - set to 'localhost' for local development or VM's public IP for remote access
HOST_TYPE=localhost

# Elasticsearch configuration
NEXT_PUBLIC_ES_HOST=http://${HOST_TYPE}:9200
NEXT_PUBLIC_ES_INDEX=cv-transcriptions

# S3 Dataset Configuration (uncomment and configure to use S3 instead of local volume)
# USE_S3_DATASET=true
# S3_BUCKET=htx-tech-test-dataset-CHANGEME
# S3_REGION=us-east-1
# AWS_DEFAULT_REGION=us-east-1
